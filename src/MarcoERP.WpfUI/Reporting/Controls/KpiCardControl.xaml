<UserControl x:Class="MarcoERP.WpfUI.Reporting.Controls.KpiCardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             FlowDirection="RightToLeft">

    <Border Style="{StaticResource CardPanel}" Padding="16" MinWidth="180" Margin="0,0,12,0">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Icon -->
            <Border Grid.Column="0" Grid.RowSpan="3"
                    Background="{Binding IconBackground, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    CornerRadius="8" Width="48" Height="48" Margin="0,0,12,0"
                    VerticalAlignment="Center">
                <materialDesign:PackIcon Kind="{Binding IconKind, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                         Width="28" Height="28" Foreground="White"
                                         HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>

            <!-- Title -->
            <TextBlock Grid.Column="1" Grid.Row="0"
                       Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       FontSize="12" Opacity="0.7" Margin="0,0,0,2" />

            <!-- Value -->
            <TextBlock Grid.Column="1" Grid.Row="1"
                       Text="{Binding Value, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       FontSize="22" FontWeight="Bold" Margin="0,0,0,2" />

            <!-- Subtitle -->
            <TextBlock Grid.Column="1" Grid.Row="2"
                       Text="{Binding Subtitle, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       FontSize="11" Opacity="0.5"
                       Visibility="{Binding Subtitle, RelativeSource={RelativeSource AncestorType=UserControl},
                                    Converter={StaticResource NullToVisibilityConverter}}" />
        </Grid>
    </Border>
</UserControl>

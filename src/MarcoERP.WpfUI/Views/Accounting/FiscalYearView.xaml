<UserControl x:Class="MarcoERP.WpfUI.Views.Accounting.FiscalYearView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             FlowDirection="RightToLeft">

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- ══════ Title + Create ══════ -->
        <Border Grid.Row="0" Style="{StaticResource CardPanel}" Padding="12,8" Margin="0,0,0,4">
            <DockPanel>
                <TextBlock Text="السنة المالية والفترات" Style="{StaticResource PageTitle}"
                           DockPanel.Dock="Right" Margin="0" VerticalAlignment="Center" />
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" HorizontalAlignment="Left"
                            VerticalAlignment="Center">
                    <TextBlock Text="إنشاء سنة:" VerticalAlignment="Center" Margin="0,0,8,0" FontSize="12" />
                    <TextBox Text="{Binding NewYear, UpdateSourceTrigger=PropertyChanged}"
                             Width="80" FontSize="14" Padding="6,4" FlowDirection="LeftToRight"
                             VerticalAlignment="Center" />
                    <Button Style="{StaticResource MaterialDesignRaisedButton}"
                            Background="{StaticResource SuccessBrush}" Foreground="White"
                            Command="{Binding CreateYearCommand}" Margin="8,0,0,0">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Plus" VerticalAlignment="Center" />
                            <TextBlock Text="إنشاء" Margin="4,0,0,0" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="12,4" />
                    <Button Style="{StaticResource MaterialDesignRaisedButton}"
                            Command="{Binding LoadCommand}" Margin="4,0">
                        <materialDesign:PackIcon Kind="Refresh" />
                    </Button>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- ══════ Error Bar ══════ -->
        <Border Grid.Row="1" Background="{StaticResource ErrorBackgroundBrush}" CornerRadius="4" Padding="10,6" Margin="8,2"
                Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon Kind="AlertCircle" Foreground="{StaticResource ErrorForegroundBrush}" VerticalAlignment="Center" />
                <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource ErrorForegroundBrush}" 
                           Margin="8,0,0,0" TextWrapping="Wrap" VerticalAlignment="Center" />
            </StackPanel>
        </Border>

        <!-- ══════ Main Content ══════ -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MinWidth="300" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="1.5*" MinWidth="400" />
            </Grid.ColumnDefinitions>

            <!-- ───── Fiscal Years List ───── -->
            <Border Grid.Column="0" Style="{StaticResource CardPanel}" Margin="0,0,2,0">
                <DockPanel>
                    <TextBlock Text="السنوات المالية" Style="{StaticResource SectionTitle}" DockPanel.Dock="Top"
                               Margin="0,0,0,8" FontSize="15" />
                    <DataGrid ItemsSource="{Binding FiscalYears}"
                              SelectedItem="{Binding SelectedYear, Mode=TwoWay}"
                              AutoGenerateColumns="False" IsReadOnly="True"
                              CanUserAddRows="False" SelectionMode="Single"
                              GridLinesVisibility="Horizontal"
                              FontSize="13" RowHeight="40" ColumnHeaderHeight="36"
                              BorderThickness="0"
                              materialDesign:DataGridAssist.CellPadding="10 8"
                              DockPanel.Dock="Top">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="السنة" Binding="{Binding Year}" Width="80"
                                                FontWeight="Bold" />
                            <DataGridTextColumn Header="من" Width="*"
                                Binding="{Binding StartDate, StringFormat=\{0:yyyy-MM-dd\}}" />
                            <DataGridTextColumn Header="إلى" Width="*"
                                Binding="{Binding EndDate, StringFormat=\{0:yyyy-MM-dd\}}" />
                            <DataGridTextColumn Header="الحالة" Width="90"
                                Binding="{Binding Status, Converter={StaticResource StatusToArabicConverter}}" />
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Year action buttons -->
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Bottom" Margin="0,8,0,0"
                                HorizontalAlignment="Center">
                        <Button Style="{StaticResource MaterialDesignRaisedButton}"
                                Background="{StaticResource PrimaryBrush}" Foreground="White"
                                Command="{Binding ActivateCommand}" Margin="4,0" MinWidth="90">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Play" VerticalAlignment="Center" />
                                <TextBlock Text="تفعيل" Margin="4,0,0,0" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource MaterialDesignRaisedButton}"
                                Background="{StaticResource DangerBrush}" Foreground="White"
                                Command="{Binding CloseCommand}" Margin="4,0" MinWidth="90">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Lock" VerticalAlignment="Center" />
                                <TextBlock Text="إقفال" Margin="4,0,0,0" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </DockPanel>
            </Border>

            <GridSplitter Grid.Column="1" Width="4" Background="Transparent"
                          HorizontalAlignment="Center" VerticalAlignment="Stretch" />

            <!-- ───── Periods ───── -->
            <Border Grid.Column="2" Style="{StaticResource CardPanel}" Margin="2,0,0,0">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="الفترات المالية" Style="{StaticResource SectionTitle}" Margin="0"
                                   VerticalAlignment="Center" />
                        <Border CornerRadius="12" Padding="12,4" Margin="12,0,0,0"
                                Background="{StaticResource InfoBrush}"
                                Visibility="{Binding SelectedYear, Converter={StaticResource NullToVisibilityConverter}}">
                            <TextBlock Foreground="White" FontSize="12" FontWeight="SemiBold">
                                <Run Text="{Binding SelectedYear.Year, Mode=OneWay}" />
                                <Run Text=" — " />
                                <Run Text="{Binding SelectedYearStatus, Mode=OneWay}" />
                            </TextBlock>
                        </Border>
                    </StackPanel>

                    <DataGrid ItemsSource="{Binding Periods}"
                              SelectedItem="{Binding SelectedPeriod, Mode=TwoWay}"
                              AutoGenerateColumns="False" IsReadOnly="True"
                              CanUserAddRows="False" SelectionMode="Single"
                              GridLinesVisibility="Horizontal"
                              FontSize="13" RowHeight="40" ColumnHeaderHeight="36"
                              BorderThickness="0"
                              materialDesign:DataGridAssist.CellPadding="10 8"
                              DockPanel.Dock="Top">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="#" Binding="{Binding PeriodNumber}" Width="50" />
                            <DataGridTemplateColumn Header="الشهر" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock FontWeight="SemiBold">
                                            <TextBlock.Text>
                                                <Binding Path="StartDate" StringFormat="{}{0:MMMM}" />
                                            </TextBlock.Text>
                                        </TextBlock>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="من" Width="*"
                                Binding="{Binding StartDate, StringFormat=\{0:yyyy-MM-dd\}}" />
                            <DataGridTextColumn Header="إلى" Width="*"
                                Binding="{Binding EndDate, StringFormat=\{0:yyyy-MM-dd\}}" />
                            <DataGridTemplateColumn Header="الحالة" Width="90">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border CornerRadius="10" Padding="8,2" HorizontalAlignment="Center">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="{StaticResource SuccessBrush}" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="Locked">
                                                            <Setter Property="Background" Value="{StaticResource DangerBrush}" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <TextBlock Text="{Binding Status, Converter={StaticResource StatusToArabicConverter}}"
                                                       Foreground="White" FontSize="11" FontWeight="SemiBold" />
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Period actions -->
                    <Border DockPanel.Dock="Bottom" Padding="0,10,0,0">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Style="{StaticResource MaterialDesignRaisedButton}"
                                        Background="{StaticResource WarningBrush}" Foreground="White"
                                        Command="{Binding LockPeriodCommand}" Margin="4,0" MinWidth="100">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="Lock" VerticalAlignment="Center" />
                                        <TextBlock Text="قفل الفترة" Margin="4,0,0,0" VerticalAlignment="Center" />
                                    </StackPanel>
                                </Button>
                                <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                                        Command="{Binding UnlockPeriodCommand}" Margin="4,0" MinWidth="100">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="LockOpen" VerticalAlignment="Center" />
                                        <TextBlock Text="فتح الفترة" Margin="4,0,0,0" VerticalAlignment="Center" />
                                    </StackPanel>
                                </Button>
                            </StackPanel>

                            <!-- Unlock reason -->
                            <StackPanel Margin="0,10,0,0">
                                <TextBlock Text="سبب فتح الفترة (مطلوب للفتح):" FontSize="12" Foreground="{StaticResource SubtitleBrush}" Margin="0,0,0,4" />
                                <TextBox Text="{Binding UnlockReason, UpdateSourceTrigger=PropertyChanged}"
                                         materialDesign:HintAssist.Hint="أدخل السبب..."
                                         FontSize="12" Padding="8,6" />
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </DockPanel>
            </Border>
        </Grid>

        <!-- ══════ Status Bar ══════ -->
        <Border Grid.Row="3" Padding="10,6" Margin="8,4,8,0">
            <DockPanel>
                <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource SubtitleBrush}" FontSize="12" />
                <ProgressBar IsIndeterminate="{Binding IsBusy}"
                             Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"
                             Height="3" DockPanel.Dock="Left" Width="100" Margin="16,0,0,0" />
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
